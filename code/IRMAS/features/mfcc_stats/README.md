MFCC statistics feature arrays used by models and notebooks.

Files:

- `X.npy` — feature array
- `y.npy` — label array

Generate with the preprocessing notebook `notebooks/preprocessing/feature_extraction.ipynb`.
